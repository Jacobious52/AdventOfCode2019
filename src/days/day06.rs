use super::advent::problem::Problem;
use crate::{part, run};

use colored::*;

pub fn part1() {
    part!(1);

    let _problem = Problem::<_, usize>::new(|_range: u64| {
        
        0
    });

    // tests
    run!();

    run!(
        
    );
}

pub fn part2() {
    part!(2);

    let _problem = Problem::<_, usize>::new(|_range: u64| {
        0
    });

    // tests
    run!();

    run!(
        
    );
}
